# Fixture Architecture

The synthetic service models a read-only order route, a minimal authentication/role boundary, and an audit record shape. It intentionally has no business-action API, outbound communications, approval queue, external dependency, deployment descriptor, or production configuration.
